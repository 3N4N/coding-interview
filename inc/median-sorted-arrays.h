#ifndef MEDIAN_SORTED_ARRAYS_H
#define MEDIAN_SORTED_ARRAYS_H

#include <iostream>
#include <stdbool.h>

using namespace std;

/**
 * Finds the median of two sorted arrays.
 */
float find_median(int *arr1, int *arr2, int size1, int size2, bool recurse);

#endif      // MEDIAN_SORTED_ARRAYS_H
