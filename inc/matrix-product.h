#ifndef MATRIX_PRODUCT_H
#define MATRIX_PRODUCT_H


#include <iostream>

/**
 * Return the max product of entries in array when going
 * toward right or down in each step and never toward left or up.
 */
int mat_prod(int **mat, int rows, int cols);

#endif      // MATRIX_PRODUCT_H
