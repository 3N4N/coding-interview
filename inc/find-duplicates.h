#ifndef FIND_DUPLICATES_H
#define FIND_DUPLICATES_H

#include <iostream>
#include <stdbool.h>
#include <map>
#include <set>
#include <list>

/**
 * Find duplicates in array of positive numbers.
 */
std::set<int> find_duplicates( int *arr, int size);


#endif      // FIND_DUPLICATES_H
